server:
  port: ${SERVER_PORT:8000}
spring:
  application:
    name: user-svc
  cloud:
    nacos:
      discovery:
        server-addr: ${REGISTRY_HOST:localhost}:${REGISTRY_PORT:8848}
      config:
        server-addr: ${REGISTRY_HOST:localhost}:${REGISTRY_PORT:8848}
        file-extension: yml
