input {
#	tcp {
#		port => 5000
#	}
	kafka {
		id => "jackson"
		bootstrap_servers => "114.67.106.146:9092"
		topics => "jackson"
		auto_offset_reset => "latest"
	}
}

## Add your filters / logstash plugins configuration here

output {
	elasticsearch {
		hosts => "elasticsearch:9200"
		user => "elastic"
		password => "changeme"
	}
}
